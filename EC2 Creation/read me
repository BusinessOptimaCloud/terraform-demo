Download main.tf and variable.tf on your server in which terraform installed 
Update the key-pair name in variable.tf file, the key-pair name should be available in your console to access the server. 
Once above steps are done run the following commands
$ terraform init
$ terraform plan
$ terraform apply
once the above commands are passed you can see the new instace been launched in your console in the name of Business optima
To delete the created instance use the below code
$ terraform destroy
